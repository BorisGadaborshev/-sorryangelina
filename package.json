{
  "name": "retro-board",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "vercel-build": "cd client && npm install && npm run build && cd ../api && npm install"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  }
} 