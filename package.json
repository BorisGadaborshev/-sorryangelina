{
  "name": "retro-board",
  "version": "1.0.0",
  "description": "Retro board application with client and server",
  "scripts": {
    "start": "concurrently \"cd server && npm run dev\" \"cd client && npm start\"",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 